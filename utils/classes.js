import { red } from "@mui/material/colors";

const classes = {
    navbarButton: {
        color: '#d32f2f',
        fontSize: '20px',
        textTransform: 'initial',
      },
};

export default classes;